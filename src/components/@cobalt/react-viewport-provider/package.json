{
  "name": "@cobalt/react-viewport-provider",
  "version": "1.0.6",
  "description": "A Viewport Provider component built for React",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "author": "Talkdesk",
  "license": "MIT",
  "scripts": {
    "build": "rollup --config ../rollup.config.js",
    "build:docs": "build-storybook --docs",
    "lint": "yarn lint:eslint && yarn lint:tsc",
    "lint:eslint": "eslint --ext .js,.ts,.tsx src/",
    "lint:tsc": "tsc --noEmit",
    "start": "yarn build --watch",
    "test": "jest",
    "storybook": "start-storybook"
  },
  "peerDependencies": {
    "react": "^16.9.0"
  },
  "dependencies": {
    "classcat": "^4.0.2",
    "lodash.throttle": "^4.1.1"
  },
  "devDependencies": {
    "@cobalt-utils/storybook-addon-theme-selector": "^1.0.0",
    "@cobalt/storybook-addon-docs": "^1.1.1",
    "react": "^16.9.0"
  }
}
